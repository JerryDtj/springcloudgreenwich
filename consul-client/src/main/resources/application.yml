server:
  port: 10013
spring:
  application:
    name: consul-client
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        health-check-url: http://localhost:10013/actuator/health
        service-name: consul-client
    discovery:
      client:
        health-indicator:
          enabled: true
